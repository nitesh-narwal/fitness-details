spring:
  application:
    name: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
      #service-url:
       # defaultZone: http://localhost:8761/eureka/
